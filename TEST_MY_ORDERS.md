# 我的订单功能测试指南

## 功能说明

用户可以查看自己的所有订单，包括：
- 订单列表展示
- 订单详情查看
- 订单支付（模拟）
- 订单删除
- 订单评价

## 测试步骤

### 1. 访问订单列表
1. 登录用户账号
2. 点击右上角用户头像或菜单
3. 选择"我的订单"
4. 或直接访问：`http://localhost:3000/user/orders`

### 2. 查看订单列表
订单列表显示内容：
- ✅ 订单号
- ✅ 下单时间
- ✅ 审核状态（已审核/待审核）
- ✅ 支付状态（已支付/未支付）
- ✅ 美食名称
- ✅ 店铺名称
- ✅ 联系电话
- ✅ 购买数量
- ✅ 单价
- ✅ 总价
- ✅ 配送地址
- ✅ 联系方式

### 3. 订单操作
每个订单卡片提供以下操作：
- **查看详情** - 跳转到订单详情页
- **去支付** - 仅未支付订单显示
- **评价** - 仅已审核订单显示
- **删除** - 删除订单

### 4. 查看订单详情
1. 点击订单卡片上的"查看详情"按钮
2. 查看完整的订单信息
3. 可以进行支付或删除操作

### 5. 刷新订单列表
点击右上角的"刷新"按钮重新加载订单数据

## 字段映射说明

后端数据库字段 → 前端显示：
- `dingdanbianhao` → 订单号
- `xiadanshijian` → 下单时间
- `meishimingcheng` → 美食名称
- `dianpumingcheng` → 店铺名称
- `lianxidianhua` → 联系电话
- `jiage` → 单价
- `goumaishuliang` → 数量
- `zongjiage` → 总价
- `yonghuzhanghao` → 用户账号
- `yonghuxingming` → 用户姓名
- `yonghushouji` → 联系方式
- `peisongdizhi` → 配送地址
- `sfsh` → 审核状态（是/否）
- `shhf` → 审核回复
- `ispay` → 支付状态（已支付/未支付）

## 调试方法

### 浏览器控制台
打开开发者工具（F12），查看：
```
订单列表：[{...}, {...}]
```

### Network 标签页
查看以下请求：
- GET `/foodie/meishidingdan/list?page=1&limit=10&yonghuzhanghao=xxx`
- GET `/foodie/meishidingdan/info/{id}`
- POST `/foodie/meishidingdan/delete`

### 后端日志
确认后端正确处理请求

## 常见问题

### 1. 订单列表为空
- 确认已经下过订单
- 检查用户账号是否正确
- 查看后端返回的数据

### 2. 字段显示为空
- 检查数据库中的字段是否有值
- 确认字段名映射是否正确

### 3. 分页不工作
- 确认订单总数超过每页显示数量
- 检查分页组件的绑定

## 修改内容总结

### 前端修改
1. `foodie-user/src/views/Order/List.vue`
   - 修复字段名映射
   - 添加支付状态标签
   - 优化显示样式

2. `foodie-user/src/views/Order/Detail.vue`
   - 修复字段名映射
   - 移除不存在的图片显示
   - 添加审核回复显示

3. `foodie-user/src/stores/order.js`
   - 移除不必要的过滤条件
   - 添加调试日志

## 下一步功能

可以继续实现：
- ✅ 订单支付功能
- ✅ 订单评价功能
- ✅ 订单筛选（按状态）
- ✅ 订单搜索
- ✅ 订单导出
