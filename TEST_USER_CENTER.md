# 用户个人中心功能测试指南

## 功能说明

用户可以在个人中心查看和修改自己的信息，包括：
- 查看用户账号（不可修改）
- 修改密码（可选）
- 修改用户姓名
- 修改性别
- 修改年龄
- 上传头像
- 修改手机号
- 修改配送地址

## 测试步骤

### 1. 访问个人中心
1. 登录用户账号
2. 点击右上角用户头像或菜单
3. 选择"个人中心"
4. 或直接访问：`http://localhost:3000/user/center`

### 2. 查看用户信息
页面加载时会自动从后端获取最新的用户信息并显示

### 3. 修改用户信息
1. 修改需要更新的字段（如姓名、手机号、地址等）
2. 如果需要修改密码，在密码框中输入新密码
3. 如果不修改密码，密码框留空即可
4. 点击"更新信息"按钮

### 4. 验证更新
1. 看到"更新成功"提示
2. 刷新页面，确认信息已更新
3. 在下单时，配送地址和联系方式会自动填充最新信息

## 修改内容说明

### 前端修改

#### 1. `foodie-user/src/stores/user.js`
```javascript
// 更新用户信息方法
const updateUserInfo = async (data) => {
  // 获取用户 ID
  const userid = localStorage.getItem('userid')
  
  // 构建更新数据
  const updateData = {
    id: userid,
    ...data
  }
  
  // 如果密码为空，删除密码字段
  if (!updateData.mima) {
    delete updateData.mima
  }
  
  // 调用后端 API
  await updateUserInfoApi(updateData)
  
  // 更新本地状态
  userInfo.value = { ...userInfo.value, ...data }
  
  // 重新获取用户信息
  await fetchUserInfo()
}
```

#### 2. `foodie-user/src/views/User/Center.vue`
- 页面加载时重新获取最新用户信息
- 提交更新时添加调试日志
- 更新成功后清空密码字段

### 后端接口

使用现有的 `/yonghu/update` 接口：
```
POST /foodie/yonghu/update
Content-Type: application/json

{
  "id": 用户ID,
  "yonghuxingming": "新姓名",
  "xingbie": "男",
  "nianling": 25,
  "yonghushouji": "13800138000",
  "peisongdizhi": "新地址",
  "mima": "新密码" // 可选，不修改密码则不传
}
```

## 数据流程

1. **页面加载**
   - 调用 `userStore.fetchUserInfo()`
   - 从后端获取最新用户信息
   - 填充到表单中

2. **提交更新**
   - 收集表单数据
   - 调用 `userStore.updateUserInfo()`
   - 发送 POST 请求到 `/yonghu/update`
   - 更新本地状态
   - 重新获取用户信息确保同步

3. **数据同步**
   - 更新成功后，本地 store 中的 userInfo 会更新
   - 下次下单时会使用最新的地址和手机号

## 调试方法

### 浏览器控制台
打开开发者工具（F12），查看：
```
提交更新的用户信息：{...}
更新用户信息：{...}
```

### Network 标签页
查看以下请求：
- GET `/foodie/yonghu/info/{id}` - 获取用户信息
- POST `/foodie/yonghu/update` - 更新用户信息

### 验证更新
1. 在个人中心修改信息
2. 刷新页面，确认显示最新信息
3. 去下单页面，确认地址和手机号已更新

## 常见问题

### 1. 更新后信息没有变化
- 检查浏览器控制台是否有错误
- 查看 Network 标签页，确认请求成功
- 检查后端是否正确处理更新请求
- 刷新页面重新加载数据

### 2. 密码修改失败
- 确认密码字段有值
- 检查后端是否正确处理密码字段
- 查看后端日志

### 3. 头像上传不工作
- 需要配置文件上传接口
- 检查上传地址是否正确

## 字段说明

| 字段名 | 说明 | 是否必填 | 备注 |
|--------|------|---------|------|
| yonghuzhanghao | 用户账号 | 是 | 不可修改 |
| mima | 密码 | 否 | 留空表示不修改 |
| yonghuxingming | 用户姓名 | 否 | |
| xingbie | 性别 | 否 | 男/女 |
| nianling | 年龄 | 否 | 1-120 |
| touxiang | 头像 | 否 | 图片URL |
| yonghushouji | 手机号 | 否 | |
| peisongdizhi | 配送地址 | 否 | |

## 测试用例

### 测试用例 1：修改基本信息
1. 修改姓名为"测试用户2"
2. 修改手机号为"13900139000"
3. 点击更新
4. 验证：刷新页面，信息已更新

### 测试用例 2：修改密码
1. 在密码框输入新密码"654321"
2. 点击更新
3. 退出登录
4. 使用新密码登录
5. 验证：能够成功登录

### 测试用例 3：修改配送地址
1. 修改配送地址为"新地址456号"
2. 点击更新
3. 去美食详情页下单
4. 验证：配送地址自动填充为新地址

### 测试用例 4：不修改密码
1. 修改姓名
2. 密码框留空
3. 点击更新
4. 退出登录
5. 使用原密码登录
6. 验证：能够成功登录（密码未改变）
